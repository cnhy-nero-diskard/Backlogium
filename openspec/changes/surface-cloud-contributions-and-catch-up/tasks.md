## 1. Durable contribution evidence

- [x] 1.1 Bump Room from v36 to v37 and commit the exported schema; add nullable, independently typed recovered-play and timing-informed contribution states to the session ledger, implement `MIGRATION_36_37`, and register it in `DatabaseModule`. Add `v36ToV37_preservesSessionTimesMinutesAndDefaultsProvenanceToUnknown` to `MigrationTest`, and extend `allMigrationsFromV13` through v37; verify the hop and chain with the connected MigrationTest job in task 4.3.
- [x] 1.2 Project both contribution facts through `SessionRepository` as domain values with no Room imports in product UI; verify a repository test distinguishes unknown, known-none, full, and partial independently for each fact and permits both facts on one session.
- [x] 1.3 Record recovered-play provenance only when cloud ingest actually credits shared-game play through the existing writer; verify accepted, rejected-gap, duplicated, and mixed live/cloud observations, preserving any timing-informed fact already on the row.
- [x] 1.4 Record timing provenance only when cloud placement changes the unaided owned-game session actions, in the existing atomic commit; verify changed/unchanged actions, rejected coverage, no reader, exact Steam-owned minute totals, and preservation of any recovered-play fact.
- [x] 1.5 Carry both independent facts across open-session extension, source conversion, and closure, distinguishing local-then-cloud and cloud-then-local partial results; verify a source-converted open recovered session later extended by owned-game placement retains both facts and neither order claims full contribution when local play contributed.
- [x] 1.6 When historical re-file changes session timing, mark each replacement session as timing-informed without clearing prior recovered/timing facts; verify apply-and-reverse restores original attribution and both original contribution facts exactly.

## 2. Routine reader and admission policy

- [x] 2.1 Add the pending-evidence Room store using `MIGRATION_37_38` and an exported v38 schema (preserve the shipped v37 migration); extend the deep-history migration chain to v38. Add a bounded, account- and reader-generation-fenced multi-page routine read through the existing serialized repository and ingest callback; apply the pending-evidence/effect-before-position protocol to every cursor-advancing path (verification, manual Read now, placement, routine catch-up, and complete-history re-file), retaining owned-game intervals and interval-boundary state before each page advances. Verify all paths preserve evidence, failed page effects leave that page retryable, and replay after effects commit but cursor persistence fails is idempotent.
- [x] 2.2 Bound one routine attempt to four pages, persist the consumed position page by page, and report partial/failed/complete/no-new-data outcomes; verify restart after a partial page sequence resumes without missing or duplicating shared-game minutes.
- [ ] 2.3 Persist Automatic/12-hour/daily/48-hour policy and last admitted routine attempt, initializing Automatic on first successful verification and reconciling already configured, previously verified readers at startup/upgrade when no policy exists; verify reconciliation needs no new verification event, seeds the initial ordering watermark, and preserves existing cloud read/ingest cursors, policy, and cooldown across reboot and preference changes.
- [ ] 2.4 Add a shared admission gate for periodic and play-end opportunities, with network-constrained unique WorkManager jobs; verify coincident events, repeated play ends, and failure cooldown, plus that a newer terminal manual/placement read satisfies only one opportunity while an older terminal read and a newer partial read do not suppress catch-up.
- [ ] 2.5 Schedule Automatic's daily safety opportunity and overrides' corresponding periodic opportunities, and enqueue an eligible catch-up after observed play ends; verify both paths defer correctly under their shared gate and an offline phone retains normal local behavior.
- [ ] 2.6 Persist and advance a reader generation, and fence/cancel pending routine work on endpoint removal, replacement, or Steam account change, while keeping manual and accuracy-driven placement reads independent of the routine gate; replacement starts a new generation and deletes prior-generation pending evidence. Explicitly delete pending evidence on reader removal and in `AccountRoomReset`; verify stale evidence cannot survive or inform a later placement, account changes fence prior-account responses, and Steam sync still credits every minute when the endpoint fails.
- [ ] 2.7 Persist a small last-read/outcome/observation-freshness summary separately from the in-memory interval snapshot; verify offline status after process restart distinguishes stale poller observations, last reader success, failure, and partial pagination without exposing credentials.
- [ ] 2.8 Add a routine-before-delta regression test: let routine catch-up retain an owned-game interval and advance the shared cursor before Steam reports its increase, then verify a later placement combines that pending interval with the unread suffix, places no more or fewer than the Steam-reported minutes, and retains evidence until the Steam baseline/session commit succeeds.
- [ ] 2.9 Add a placement-read failure/retry regression: persist an owned interval while placement advances the shared cursor, fail the Steam baseline/session commit, then retry and verify the same evidence remains available and is used only for the retry's diff window; retire it only when a successful baseline makes it inapplicable.
- [ ] 2.10 Add an interval-boundary regression: persist an ongoing interval at a terminal read, read a later close-only page, then process two Steam deltas; verify the opening boundary reconstructs and refines the same interval, each delta uses only its intersecting evidence, and no stale ongoing version or minutes are lost/doubled.
- [ ] 2.11 Prune closed pending intervals when a committed per-app Steam baseline makes them end at or before `lastSyncAt`, including a successful sync with no positive delta; verify future diff windows cannot use stale evidence while intervals extending beyond the baseline remain available.

## 3. History and Settings experience

- [ ] 3.1 Add an unobtrusive session-level cloud mark and separate accessible explanation for independent recovery, timing, and partial facts in History; verify a session carrying both facts shows both explanations, while unmarked legacy sessions, hidden games, absent reader, font scaling, and day/game expansion remain accurate.
- [ ] 3.2 Add a Cloud activity entry only when the loaded visible History period has contributed facts, and a pushed detail leading with recovered and cloud-timed sessions linked back to History; verify a dual-fact session may appear in both fact groups, counts are not described as disjoint sessions, and new/older/empty windows show the correct conditional entry.
- [ ] 3.3 Present stored reader attempt/success and observation freshness below contributions, with unknown/partial coverage and failed-read-after-success states; verify offline reopening makes no network request and never describes reader success as live poller health.
- [ ] 3.4 Add the Automatic and bounded cadence choices and routine status once in the cloud Settings destination (Data & privacy after the Settings restructuring lands), preserving Read now; verify copy explains that override limits routine reads only, not minute polling or placement reads, and that next eligibility is not a promised execution time.
- [ ] 3.5 Move new History and Settings labels/counts into Android resources and provide icon-independent accessibility semantics; verify a non-default locale and screen reader can distinguish recovered from timing-informed sessions without relying on color.

## 4. Backup and integration verification

- [ ] 4.1 Introduce backup format v2 for session contribution provenance while continuing to emit formatVersion explicitly under the existing Json configuration; require both independent fact states (`UNKNOWN`, `NONE`, `FULL`, or `PARTIAL`) on every v2 session, without exporting endpoint, bearer token, cursor, or pending intervals. Verify encoded JSON explicitly contains `formatVersion: 2`, both explicit unknown states, and a mixed recovered/timing contribution, then round-trip each state and session minutes.
- [ ] 4.2 Use one version-aware decode path for picked files and retained snapshots; support only v1 and v2 (including v1 files whose default formatVersion was omitted), and preserve legacy absence separately from v2's explicit `UNKNOWN` per fact. Reject unsupported versions, mixed-version fields, and missing/null/invalid v2 members before merge. Verify v1 absence leaves matching local facts unchanged, v2 `UNKNOWN` explicitly replaces the corresponding local fact, and v2 independent known states replace both facts (including a dual-fact session), with minutes and natural keys unchanged.
- [ ] 4.3 Run `.\gradlew.bat --no-daemon :gamification:test :app:testDebugUnitTest`, `.\gradlew.bat --no-daemon :app:compileDebugKotlin`, and `.\gradlew.bat --no-daemon :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.backlogium.data.local.MigrationTest` on a connected emulator/device; verify all pass, including the v36-to-v37 and v37-to-v38 cases and v13-to-current chain through v38, and the `CLAUDE.md` UI storage-boundary and haptics checks report no new breach.
- [ ] 4.4 Exercise a configured, offline, stale, failing, and multi-page catch-up with realistic session history on a device; verify displayed contributions match stored evidence, cloud remains optional, and no new routine trigger modifies the poller's server-side schedule.
