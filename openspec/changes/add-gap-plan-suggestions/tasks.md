## 1. Steam Suggestion Metadata Storage

- [x] 1.1 Add a nullable participation-category payload to the Store metadata cache and a separate Steam review-summary cache entity/DAO with a cascading foreign key to `games`, register both in Room, export the new schema, and verify fresh-install schema tests pass.
- [x] 1.2 Add the forward Room migration that preserves existing genre/app-type rows with unknown categories and creates the review cache, widen Store enrichment eligibility so a row whose category payload is null is selected regardless of its `checkedAt` freshness, and verify `MigrationTest` covers retained old rows, null-category rows being immediately eligible again, and writable upgraded tables.
- [x] 1.3 Confirm the account-change and backup paths need no new exclusion logic — the backup export format is an enumerated allowlist that never carried the genre cache, and a review cache with `ON DELETE CASCADE` is already cleared when `AccountRoomReset` deletes games — and verify backup round-trip and account-change tests remain green with the new tables present.

## 2. Steam Metadata Acquisition

- [x] 2.1 Capture real `appdetails` fixtures for a known multiplayer, online co-op, massively multiplayer, and single-player-only app, pin the participation-category identifier set from those fixtures into one named constant table, then extend the narrow Store DTO (numeric category ids, distinct from the string genre ids), parser result, category codec, genre enrichment, and family-shared admission seeding to retain categories separately from genres — writing null for a refused envelope and an empty list only for a definitive "advertises none" — and verify fixtures cover each category kind, malformed, absent, and refused responses.
- [x] 2.2 Add a credential-free Steam review-summary endpoint and narrow DTO/parser for description and positive, negative, and total counts, confirming from a captured response which page-size parameter suppresses review bodies, and verify data-source tests distinguish valid, definitive unavailable, refused-envelope, malformed, HTTP, and transport outcomes.
- [x] 2.3 Add repository domain models and observable cache mappings that preserve missing, unavailable, and available review/category states without leaking Room types, keeping unknown categories distinct from "checked, advertises none", and verify repository tests cover all states and transient-failure retention.
- [x] 2.4 Implement missing-first 30-day review enrichment in batches of at most 25 with at least 500 ms request spacing, a 15-minute continuation delay, network constraints, and backoff independent of authenticated sync, and verify worker/scheduler tests cover bounds, freshness, hidden-game exclusion, rate-limit responses ending a batch without recording an absence, retry, and continuation.

## 3. Pure Selection Engine

- [x] 3.1 Revise the domain models for one pick per tier: replace the multi-member variant with a single optional pick, add the generation seed, drop the bundle-level value and diversity types, and verify JVM tests still cover input validation, the 1,095-day horizon, and 70/85/100 percent capacity shares.
- [x] 3.2 Implement source-aware remaining-work eligibility for visible owned and family-shared games, request category toggles, selected-basis HLTB coverage reporting, and Full-budget fit, and verify tests cover hidden-by-input games, partial progress, overflow safety, complete games, and missing estimates without any lookup dependency.
- [x] 3.3 Demote review quality, genre affinity, and completion momentum from selection inputs to presented facts: keep their derivations and their fact-bearing output, remove the weighted `candidateQuality` composite and the neutral constants that only existed to keep a score comparable, and verify tests assert these signals no longer influence which candidate is chosen.
- [x] 3.4 Implement per-tier target selection: draw uniformly at random, from a seeded generator, among the eligible candidates nearest a tier's share of capacity, enforcing distinctness across the three tiers, and verify tests cover tiers yielding different lengths, an exhausted pool leaving a tier empty, distinctness when one game is nearest for two tiers, and identical inputs plus an identical seed producing identical picks.
- [x] 3.5 Delete the bundle composer — beam search, the 32-entry frontier, `bundleValue`, budget utilization, pairwise genre diversity, and the finished-bundle tie-break chain — along with the tests that exist only to pin them, and verify the remaining suite still builds and passes.
- [x] 3.6 Remove the remove-and-replace editing surface and its validation, which the single-pick result supersedes, and verify nothing else depends on the retained eligible pool for per-slot replacement.

## 4. Aggregation And Live Enrichment

- [x] 4.1 Add one repository-level gap-plan feed that joins visible `LibraryGame` values, Personal Pace, all-time tracked minutes per game, windowed completed-session projections, and suggestion metadata into domain inputs, deriving the forecast range from a freshly read current date rather than the pace repository's construction-time date, and verify feed tests cover reliable pace, required manual hours while learning, truthful family-shared playtime, a date rollover between construction and generation, and offline cache gaps.
- [x] 4.2 Reduce bounded current-player decoration to the multiplayer games among the three finalized picks — at most three app ids, concurrency 4, one 8-second overall window — with no lookup for single-player picks, and verify tests cover successful zero, unavailable counts, a wholly single-player result issuing no lookup at all, the three-id ceiling, and no lookup being issued before finalization.
- [x] 4.3 Guard every generation and live lookup with identity-based ownership so cancellation, navigation, regeneration, and late timeout results cannot publish over a successor, and verify coroutine tests reproduce superseded same-request and independent-request races.
- [x] 4.4 Apply available counts only to a pick's factual presentation, and verify tests prove that the same request and seed with every lookup succeeding and with every lookup failing yield identical games in identical tiers, differing only in the presence of a player count.

## 5. Collection Adoption

- [x] 5.1 Map an accepted snapshot to a `CollectionSaveDraft` with `id = 0`, name `Before <anticipated title>`, deadline mode, the deadline mode's default sort, ISO target date, null accent and description, selected basis, exact distinct membership, and no done marks, and verify mapping tests cover Story, Completionist, edited previews, and family-shared members.
- [x] 5.2 Save the mapped draft through the existing database transaction and return the created collection id while preserving the preview on failure, and verify integration tests prove collection-plus-membership atomicity, retry behavior, and stable membership after recommendation inputs change.

## 6. Gap-Plan UI

- [x] 6.1 Revise ViewModel state and actions for three single picks and a retained seed: replace the variant-and-member state with one pick per tier, expose a single reroll action that draws a new seed, remove the replacement-offer actions, and verify ViewModel tests cover reliable, learning, offline, empty, reroll-changes-the-picks, reroll-cannot-vary, save-success, and save-failure flows.
- [x] 6.2 Build an adaptive setup surface for anticipated title, future date within the planning horizon, Story/Completionist intent, unplayed/started inclusion, and learning-only total hours, and verify Compose behavior tests cover validation, horizon rejection, defaults, manual-hours visibility, narrow layouts, and state restoration within the route.
- [x] 6.3 Rebuild the result surface as three single-game cards that state the request's full forecast capacity once and each tier's own share, and carry the pick's remaining time, Store genres, Steam review description with count, current players for a multiplayer pick, and a family-shared label, and verify Compose tests cover complete, sparse, offline, and empty-tier states plus a Relaxed card whose withheld capacity stays visible.
- [x] 6.4 Add in-place inspection: activating a pick opens the existing collection game-detail overlay at partial height over the result, dismissible by its control and system back, returning to unchanged picks, and verify UI tests cover opening, both dismissal routes, and that inspection never rerolls.
- [x] 6.5 Reduce the result to exactly one control that produces a new set, remove the duplicate regenerate control, surface the pool-too-small-to-vary state, and verify UI tests cover a reroll changing the picks, the unchanged-pool state explaining itself, review-save, retry, and open-created-collection without haptics outside the shared vocabulary.
- [x] 6.6 Register one pushed gap-plan route and add entry actions on Home and Collections without changing bottom navigation, and verify navigation tests open the same builder from both surfaces and return/open the created collection correctly.

## 7. Verification

- [ ] 7.1 Run `./gradlew :app:testDebugUnitTest` and fix all selection, repository, worker, ViewModel, migration, and regression test failures.
- [ ] 7.2 Run `./gradlew assembleDebug` and verify the debug APK compiles with Room schema export, Hilt bindings, WorkManager registration, navigation, and Compose resources.
- [ ] 7.3 Run the repository UI-to-storage import boundary grep and haptic-authority grep documented in `CLAUDE.md`, and verify the new product UI imports no Room/DAO/DataStore types and introduces no platform haptic call.
- [ ] 7.4 Exercise on a device or emulator with reliable pace, learning/manual budget, offline mode, missing HLTB/reviews, family sharing, multiplayer decoration, and accepted collection save; confirm the three picks are three distinct games of visibly different lengths, that rebuilding changes them, that opening a pick's detail overlay and dismissing it leaves them unchanged, and that all three cards stay readable on narrow and wide layouts.
- [x] 7.5 Exercise an upgraded database seeded with freshly checked pre-change metadata rows and confirm participation categories are acquired on the next enrichment run rather than after the 30-day window, so the multiplayer path is live on an existing install.
- [x] 7.6 Update the `steam-player-count` capability's Purpose during spec sync so it no longer scopes current-player lookups to the game detail screen; the delta's requirement change alone does not carry that prose.
