## 1. Steam Suggestion Metadata Storage

- [x] 1.1 Add a nullable participation-category payload to the Store metadata cache and a separate Steam review-summary cache entity/DAO with a cascading foreign key to `games`, register both in Room, export the new schema, and verify fresh-install schema tests pass.
- [x] 1.2 Add the forward Room migration that preserves existing genre/app-type rows with unknown categories and creates the review cache, widen Store enrichment eligibility so a row whose category payload is null is selected regardless of its `checkedAt` freshness, and verify `MigrationTest` covers retained old rows, null-category rows being immediately eligible again, and writable upgraded tables.
- [x] 1.3 Confirm the account-change and backup paths need no new exclusion logic — the backup export format is an enumerated allowlist that never carried the genre cache, and a review cache with `ON DELETE CASCADE` is already cleared when `AccountRoomReset` deletes games — and verify backup round-trip and account-change tests remain green with the new tables present.

## 2. Steam Metadata Acquisition

- [x] 2.1 Capture real `appdetails` fixtures for a known multiplayer, online co-op, massively multiplayer, and single-player-only app, pin the participation-category identifier set from those fixtures into one named constant table, then extend the narrow Store DTO (numeric category ids, distinct from the string genre ids), parser result, category codec, genre enrichment, and family-shared admission seeding to retain categories separately from genres — writing null for a refused envelope and an empty list only for a definitive "advertises none" — and verify fixtures cover each category kind, malformed, absent, and refused responses.
- [x] 2.2 Add a credential-free Steam review-summary endpoint and narrow DTO/parser for description and positive, negative, and total counts, confirming from a captured response which page-size parameter suppresses review bodies, and verify data-source tests distinguish valid, definitive unavailable, refused-envelope, malformed, HTTP, and transport outcomes.
- [x] 2.3 Add repository domain models and observable cache mappings that preserve missing, unavailable, and available review/category states without leaking Room types, keeping unknown categories distinct from "checked, advertises none", and verify repository tests cover all states and transient-failure retention.
- [x] 2.4 Implement missing-first 30-day review enrichment in batches of at most 25 with at least 500 ms request spacing, a 15-minute continuation delay, network constraints, and backoff independent of authenticated sync, and verify worker/scheduler tests cover bounds, freshness, hidden-game exclusion, rate-limit responses ending a batch without recording an absence, retry, and continuation.

## 3. Pure Gap-Plan Engine

- [x] 3.1 Add plain domain request, capacity provenance, candidate, reason, intensity, variant, and snapshot models with Story/Completionist mapping, and verify JVM tests cover input validation, the 1,095-day target-date horizon, and 70/85/100 percent budget rounding.
- [x] 3.2 Implement source-aware remaining-work eligibility for visible owned and family-shared games, request category toggles, selected-basis HLTB coverage reporting, and Full-budget fit, and verify tests cover hidden-by-input games, partial progress, overflow safety, complete games, and missing estimates without any lookup dependency.
- [x] 3.3 Implement the 95% Wilson review-quality component with an explicit 0.5 neutral for missing data and fact-bearing reason output, and verify tests show established high-volume ratings outrank tiny perfect samples, absent ratings remain neutral and undisclosed, and a library with no cached reviews or genres still ranks and explains itself on progress and fit alone.
- [x] 3.4 Implement 56-completed-day, 28-day-half-life genre affinity using at most one contribution per game/date/genre plus selected-basis completion momentum, and verify tests cover recency weighting, marathon resistance, no history, unknown genres, unplayed games, and partial progress.
- [x] 3.5 Implement deterministic one-to-five-game bundle scoring and beam composition for each intensity with a fixed candidate order, a frontier of 32 partial combinations keyed on partial `bundleValue`, and the declared tie-break chain, enforcing budget, uniqueness, utilization against the variant's own budget, neutral unknown genres, and pairwise genre diversity, and verify tests cover adversarial packing, the five-game cap, empty/one-game plans, diversity preference, app-id tie-breaking, and byte-identical repeat runs.
- [x] 3.6 Add pure remove-and-replace validation against the retained eligible pool, recalculating planned/reserve minutes without mutating the other choices, and verify tests reject duplicate or over-budget replacements and preserve stable accepted membership.

## 4. Aggregation And Live Enrichment

- [x] 4.1 Add one repository-level gap-plan feed that joins visible `LibraryGame` values, Personal Pace, all-time tracked minutes per game, windowed completed-session projections, and suggestion metadata into domain inputs, deriving the forecast range from a freshly read current date rather than the pace repository's construction-time date, and verify feed tests cover reliable pace, required manual hours while learning, truthful family-shared playtime, a date rollover between construction and generation, and offline cache gaps.
- [x] 4.2 Finalize all three variants from local state before any network call, then add bounded current-player decoration for the distinct multiplayer games those variants contain — at most fifteen app ids, concurrency 4, one 8-second overall window — with no lookup for single-player members, and verify tests cover successful zero, unavailable counts, members shared across variants, the fifteen-id ceiling, and no lookup being issued before finalization.
- [x] 4.3 Guard every generation and live lookup with identity-based ownership so cancellation, navigation, regeneration, and late timeout results cannot publish over a successor, and verify coroutine tests reproduce superseded same-request and independent-request races.
- [x] 4.4 Apply available counts only to multiplayer row order within a variant and to factual explanations, and verify tests prove that the same request with every lookup succeeding and with every lookup failing yields identical membership in identical variants, differing only in row order and explanation chips.

## 5. Collection Adoption

- [ ] 5.1 Map an accepted snapshot to a `CollectionSaveDraft` with `id = 0`, name `Before <anticipated title>`, deadline mode, the deadline mode's default sort, ISO target date, null accent and description, selected basis, exact distinct membership, and no done marks, and verify mapping tests cover Story, Completionist, edited previews, and family-shared members.
- [ ] 5.2 Save the mapped draft through the existing database transaction and return the created collection id while preserving the preview on failure, and verify integration tests prove collection-plus-membership atomicity, retry behavior, and stable membership after recommendation inputs change.

## 6. Gap-Plan UI

- [ ] 6.1 Add ViewModel state and actions for setup validation, Personal Pace/manual capacity provenance, generation ownership, three finalized variants, transient edits, save confirmation, busy/error recovery, and created-collection navigation, and verify ViewModel tests cover reliable, learning, offline, empty, regenerate, edit, save-success, and save-failure flows.
- [ ] 6.2 Build an adaptive setup surface for anticipated title, future date within the planning horizon, Story/Completionist intent, unplayed/started inclusion, and learning-only total hours, and verify Compose behavior tests cover validation, horizon rejection, defaults, manual-hours visibility, narrow layouts, and state restoration within the route.
- [ ] 6.3 Build distinct Relaxed, Balanced, and Full result cards that state the request's full forecast capacity once and each variant's own budget, planned, and reserve time alongside capacity source, up to five game cards, family-shared labels, factual reason chips, and missing-data disclosures, and verify Compose tests cover complete, sparse, offline, empty, and incomplete-HLTB states plus a Relaxed plan whose withheld capacity stays visible.
- [ ] 6.4 Add remove, budget-valid replacement, regenerate, review-save, retry, and open-created-collection interactions without haptics outside the shared vocabulary, and verify UI tests cover budget rejection, stable snapshots, late enrichment reordering rows without changing membership, superseded enrichment, confirmation contents, and recoverable save failure.
- [ ] 6.5 Register one pushed gap-plan route and add entry actions on Home and Collections without changing bottom navigation, and verify navigation tests open the same builder from both surfaces and return/open the created collection correctly.

## 7. Verification

- [ ] 7.1 Run `./gradlew :app:testDebugUnitTest` and fix all planner, repository, worker, ViewModel, migration, and regression test failures.
- [ ] 7.2 Run `./gradlew assembleDebug` and verify the debug APK compiles with Room schema export, Hilt bindings, WorkManager registration, navigation, and Compose resources.
- [ ] 7.3 Run the repository UI-to-storage import boundary grep and haptic-authority grep documented in `CLAUDE.md`, and verify the new product UI imports no Room/DAO/DataStore types and introduces no platform haptic call.
- [ ] 7.4 Exercise on a device or emulator with reliable pace, learning/manual budget, offline mode, missing HLTB/reviews, family sharing, multiplayer decoration, regeneration, and accepted collection save, and record that all three variants remain readable and stable on narrow and wide layouts.
- [ ] 7.5 Exercise an upgraded database seeded with freshly checked pre-change metadata rows and confirm participation categories are acquired on the next enrichment run rather than after the 30-day window, so the multiplayer path is live on an existing install.
- [ ] 7.6 Update the `steam-player-count` capability's Purpose during spec sync so it no longer scopes current-player lookups to the game detail screen; the delta's requirement change alone does not carry that prose.
