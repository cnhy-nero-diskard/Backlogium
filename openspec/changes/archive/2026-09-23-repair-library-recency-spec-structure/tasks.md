## 1. Restore the recency precedence contract

- [x] 1.1 Complete the mutually exclusive recency-states requirement with its established precedence and an explicit scenario; verify a game with overlapping conditions receives only the highest-precedence state.
- [x] 1.2 Remove the stray precedence fragment from the acquisition announcement while preserving all acquisition scenarios; verify last-played and baseline requirements remain unchanged.

## 2. Validate the capability

- [x] 2.1 Run `openspec validate library-recency --type spec` and `openspec validate --specs`; verify both pass.
