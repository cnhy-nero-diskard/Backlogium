## 1. Build Variant Identity

- [x] 1.1 Configure the debug build with `.debug` application-ID and `-debug` version-name suffixes while leaving the release ID and code namespace unchanged; verify Gradle's debug and release variant metadata resolve to `com.example.backlogium.debug` and `com.example.backlogium` respectively.
- [x] 1.2 Add the debug-only `Backlogium Debug` label through the debug resource source set; verify merged debug resources use the new label and merged release resources still use `Backlogium`.

## 2. Debug Launcher Badge

- [x] 2.1 Add debug-only adaptive and round launcher resources that preserve the release artwork and overlay a high-contrast amber `DBG` badge with dark outline inside the safe zone; verify `:app:processDebugResources` succeeds without changing main/release launcher assets.
- [x] 2.2 Provide badge geometry for monochrome/themed presentation and render or inspect adaptive, round, and themed debug icons at launcher scale to verify the badge remains legible and is not clipped.

## 3. Variant-Aware Tests and Tooling

- [x] 3.1 Replace the instrumentation test's fixed release package assertion with the generated target application ID; run the focused `ExampleInstrumentedTest` against the debug variant and verify it passes with `com.example.backlogium.debug`.
- [x] 3.2 Update the `.codex`, `.claude`, and `.cline` run-on-device skill copies to launch the debug package and document one-time cleanup of legacy debug installs; verify a repository search finds no device-launch command that targets the release package after `installDebug`.
- [x] 3.3 Review remaining `com.example.backlogium` literals and retain source namespaces, CI test-class selectors, and private intent-extra keys intentionally; verify only installed-target assumptions are changed.

## 4. Build and Device Verification

- [x] 4.1 Run `:app:assembleDebug`, `:app:assembleRelease`, `:app:testDebugUnitTest`, and `:gamification:test`; verify both APKs build, unit tests pass, and APK manifests report the expected distinct application IDs and provider authorities.
- [x] 4.2 On a device that already has the signed release installed, run the debug install-and-launch workflow; verify both launcher entries exist, show the correct labels and icons, and open their corresponding package IDs.
- [x] 4.3 Configure distinguishable state in each installed variant and exercise permissions, notifications, and scheduled sync; verify state and runtime behavior remain independent while existing debug-only update gating remains intact.
- [x] 4.4 Uninstall the debug variant and verify the release app and its data still work, then reinstall debug and confirm it starts with independent state; record device/emulator evidence and any verification limitation.

## Verification evidence

- **Builds (4.1):** `gradlew :app:assembleDebug :app:assembleRelease :app:testDebugUnitTest :gamification:test --console=plain` — BUILD SUCCESSFUL in 2m 53s, 109 tasks, 0 failures.
- **APK identity (1.1, 1.2, 4.1):** `aapt dump badging` — debug APK: `package: name='com.example.backlogium.debug' versionName='0.0.0-dev-debug'`, label `Backlogium Debug`; release APK: `name='com.example.backlogium' versionName='0.0.0-dev'`, label `Backlogium`. Merged manifests: debug provider authority `com.example.backlogium.debug.androidx-startup`, release `com.example.backlogium.androidx-startup`.
- **Launcher resources (2.1):** `:app:processDebugResources` succeeded; `aapt2 dump resources` on the debug APK lists `ic_launcher_badge_debug`, `ic_launcher_badge_outline_debug`, `ic_launcher_foreground_debug`, `ic_launcher_monochrome_debug` alongside the unchanged main `ic_launcher_foreground` PNGs. Main/release launcher assets untouched (all overrides live under `app/src/debug/res`).
- **Instrumentation (3.1):** `gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.backlogium.ExampleInstrumentedTest` on Medium_Phone_API_35 emulator — 1 test, 0 failures/errors; assertion now resolves against generated `BuildConfig.APPLICATION_ID` (`com.example.backlogium.debug`).
- **Tooling (3.2, 3.3):** all three synchronized run-on-device skill copies (`.codex`, `.claude`, `.cline`) now launch `com.example.backlogium.debug` and document one-time legacy debug cleanup; repo search finds no `monkey -p com.example.backlogium` launch targeting the release package. Remaining `com.example.backlogium` literals are source namespaces, the CI `MigrationTest` class selector, and private intent keys — intentionally retained.
- **Side-by-side install (4.2):** on emulator-5554, `adb install app-debug.apk` succeeded while `com.example.backlogium` was already installed; drawer screenshot (`build/backlogium_drawer.png`) shows both launcher entries side by side — release `Backlogium` with plain artwork, debug `Backlogium …` (truncated "Backlogium Debug" label) with a legible amber `DBG` badge in the lower-right, unclipped. Each entry launches its own package: `topResumedActivity` showed `com.example.backlogium/.MainActivity` and `com.example.backlogium.debug/.MainActivity` respectively.
- **Badge icon rendering (2.2):** badge inspected at launcher scale in the adaptive (squircle) drawer rendering above — legible and inside the mask. The round icon carries the identical adaptive layers, and geometry was kept inside the 66dp safe-zone circle (badge reach ≈31.2dp from center), so the round mask clips nothing. Monochrome limitation: this launcher's build did not honor the `themed_icons` secure setting via adb (needs the Wallpaper & style UI toggle), so themed rendering was not visually confirmed; the `<monochrome>` layer (badge outline ring + DBG lettering, no amber fill) is verified present in the debug APK and follows the same safe-zone geometry.
- **State & runtime isolation (4.3):** separate UIDs (`com.example.backlogium`=u0a212/uid 10212, `com.example.backlogium.debug`=u0a213/uid 10213); separate `dataDir`s (`/data/user/0/com.example.backlogium[.debug]`); debug created its own `databases/backlogium.db`. Runtime permission state is independent: POST_NOTIFICATIONS `granted=true` on the release-id package vs `granted=false` on the fresh debug install (its own prompt shown). `dumpsys jobscheduler` shows independent WorkManager `SystemJobService` jobs per package (debug job #u0a213/0 ran and called `jobFinished`; release-id jobs #u0a212/1,25,26 scheduled separately). Debug-only update gating is compile-time (`BuildConfig.DEBUG`) and untouched.
- **Uninstall/reinstall (4.4):** `adb uninstall com.example.backlogium.debug` → Success; the release-id app still launched (`Events injected: 1`, resumed `com.example.backlogium/.MainActivity`) with its `databases`, `files`, and `shared_prefs` intact. Reinstalling the debug APK produced a fresh data dir that repopulated with its own `backlogium.db` on first launch.
- **Environment caveat:** the device occupied by `com.example.backlogium` on this emulator is a debug-signed install (versionName `0.0.0-dev`), matching the "legacy debug install at the release ID" migration case documented in the design and the run-on-device skills; behavior for a signed release at that ID is identical from Android's identity standpoint.
