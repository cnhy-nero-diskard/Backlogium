<?xml version="1.0" encoding="utf-8"?>
<!--
    Keep platform backup explicit. The app's export/import model is the only backup channel for
    library data, settings, credentials, and automatic snapshots.
-->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="backup_snapshots" />
</full-backup-content>
